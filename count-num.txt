//count the numbers in give number
#include<stdio.h>
int main()
{
    int a=setbit(10);
    printf("%d",a);
}
int setbit(int n)
{
    int count=0;
    while(n)
    {
        count=count+(n&1);
        n=n>>1;
    }
    return count;
}
